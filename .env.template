# DeepSearchAgent API Keys
# Please fill in your own API keys before use

# LiteLLM configuration - for LLM access
LITELLM_MASTER_KEY="your-litellm-key"
# Optional: Base URL if using a custom LiteLLM proxy/endpoint
LITELLM_BASE_URL=""

# Serper API - for web search
SERPER_API_KEY="your-serper-api-key"

# Jina AI API - for content processing and embedding/reranking
JINA_API_KEY="your-jina-api-key"

# WolframAlpha - for computational and mathematical processing (optional)
WOLFRAM_ALPHA_APP_ID="your-wolfram-alpha-app-id"

# Note: Other configurations like model IDs, agent parameters (max_steps, etc.)
# should now be managed in your config.yaml file (copied from config.yaml.template). 